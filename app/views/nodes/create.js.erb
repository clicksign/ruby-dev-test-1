<% if @node_new.new_record? %>
  Swal.fire({title: "Error", message: "<%= @node_new.errors.full_messages.join(', ') %>", icon: "error"})
<% else %>
  Swal.fire({title: "Success", icon: "success"})
  window.location.reload()
<% end %>