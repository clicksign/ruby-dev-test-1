<h3>Path "/" </h3>
<%= link_to 'Novo diretório', new_folder_path, role: "button" %>
<hr>
<ul>
  <% @folders.each do |folder| %>
    <li><%= image_tag folder_icon(folder) %> <%= link_to folder.name, folder_path(folder) %></li>
  <% end %>
</ul>
<hr>