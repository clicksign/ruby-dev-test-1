<p id="notice"><%= notice %></p>

<table>

  <thead>
    <tr>
      <th colspan="3"></th>
      <th>Nome</th>
    </tr>
  </thead>

  <tbody>
    <% @folders.each do |folder| %>
      <tr>
        <td>
          <%= link_to(image_tag('folder.png',
            width: '12%',
            height: '12%'
           ), folder) 
          %>
        </td>
        <td><%= link_to(folder.name, folder) %></td>
      </tr>
    <% end %>
  </tbody>

</table>

<br>

<%= link_to 'New Folder', new_folder_path %>
