<h2><%= ".../#{@folder.name}" %></h2>

<p class="text-success"><%= notice %></p>
<p class="text-danger"><%= alert %></p>

<%= render 'table' %>

<br>

<%= link_to "New folder", new_folder_folder_path(@folder), class: 'btn btn-info' %> |
<%= link_to "New document", new_folder_document_path(@folder), class: 'btn btn-info' %>